<h3>Index of all templates</h3>


<ul>

    <% @templates.each do |d| %>

        <li class="template_index">
            <%= link_to d.title, template_path(d.id) %>

        </li>

    <% end %>

</ul>